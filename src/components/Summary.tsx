import React from 'react';
import Layout from "./Layout";

const Summary: React.FC = () => {
    return (
        <Layout>
            <div>
                <h1>Summary Page</h1>
            </div>
        </Layout>

    );
};

export default Summary;
